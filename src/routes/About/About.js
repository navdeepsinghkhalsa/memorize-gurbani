import React from 'react';
// Components
import TitleBar from '../../modules/TitleBar';

const About = () => (
  <React.Fragment>
    <TitleBar text="About" />
  </React.Fragment>
);

export default About;
