import React from 'react';
// Components
import TitleBar from '../../modules/TitleBar';

const Home = () => (
  <React.Fragment>
    <TitleBar text="Memorize Gurbani" />
  </React.Fragment>
);

export default Home;
